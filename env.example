# =============================================================================
# xAI/Grok API Configuration
# Get your API key from https://console.x.ai
# This single key powers ALL xAI services: Grok, STT, TTS, Agent Tools
# =============================================================================
XAI_API_KEY=your_xai_api_key_here

# =============================================================================
# xAI User Preferences / Collections (Optional but recommended)
# Enables learning from user feedback to personalize future music generation
# Create a Management API key at: https://console.x.ai > API Keys > Management Key
# Required permissions: Collections, Files
# =============================================================================
XAI_MANAGEMENT_API_KEY=your_management_api_key_here

# Collection ID for storing user preferences (created via xAI Management API)
# Create at: https://management-api.x.ai/v1/collections
XAI_PREFERENCES_COLLECTION_ID=your_collection_id_here

# Model Configuration (task-specific routing for optimal performance)
# -----------------------------------------------------------------------------
# CREATIVE: Complex creative tasks (music generation, image-to-music, full tracks)
#   - Uses reasoning mode for best quality output
#   - 2M token context, lowest hallucination rate
XAI_MODEL_CREATIVE=grok-4-1-fast-reasoning

# AGENT: Tool-calling tasks (x_search, web_search, code_execution, collections)
#   - Optimized specifically for Agent Tools API
XAI_MODEL_AGENT=grok-4-1-fast

# SIMPLE: Fast responses (voice feedback, error messages, status text)
#   - No reasoning overhead, fastest response time
XAI_MODEL_SIMPLE=grok-4-1-fast-non-reasoning

# Legacy single model (deprecated - use task-specific models above)
XAI_MODEL=grok-4-1-fast-reasoning

# =============================================================================
# xAI Voice Configuration
# =============================================================================
# Default TTS voice for audio feedback (Ara, Rex, Sal, Eve, Una, Leo)
XAI_TTS_VOICE=Eve

# =============================================================================
# X Platform API (console.x.com / developer.x.com)
# Enables "Share to X" and X sentiment analysis features
# =============================================================================

# App-Only Bearer Token (for reading public tweets, trends, search)
# Get from: X Developer Portal > Your App > Keys and Tokens
X_API_BEARER_TOKEN=your_bearer_token_here

# OAuth 2.0 Credentials (for posting tweets on behalf of users)
# Get from: X Developer Portal > Your App > User authentication settings
# Required scopes: tweet.read, tweet.write, users.read, offline.access
X_CLIENT_ID=your_client_id_here
X_CLIENT_SECRET=your_client_secret_here
X_CALLBACK_URL=http://localhost:3001/api/x/callback

# =============================================================================
# Server Configuration
# =============================================================================
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
APP_URL=http://localhost:3001

# =============================================================================
# Rate Limiting
# =============================================================================
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW=60000

# =============================================================================
# Weather API (Optional - for context-aware music generation)
# Get a free API key from https://openweathermap.org/api
# If not set, weather context will use neutral defaults
# =============================================================================
OPENWEATHER_API_KEY=your_openweather_api_key_here
